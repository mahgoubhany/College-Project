<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Student Login & Registration - College Management System</title>
  <!-- <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"> -->
    <link href="https://cdn.jsdelivr.net/npm/bootswatch@5.3.3/dist/flatly/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
  <style>
    body {
      background: linear-gradient(120deg, #e0e7ff 0%, #f8fafc 100%);
      min-height: 100vh;
      background-image: url('https://plus.unsplash.com/premium_photo-1691962725001-8e9157a933cd?w=600&auto=format&fit=crop&q=60&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxzZWFyY2h8MTkzfHxzdHVkZW50c3xlbnwwfHwwfHx8MA%3D%3D');
      background-repeat: no-repeat;
    background-size: cover;           /* Fills the screen */
    background-position: center;      /* Keeps the image centered */
    background-attachment: fixed;     /* Optional: keeps background still on scroll */
    min-height: 100vh;
    }
    .card {
      border: none;
      border-radius: 1rem;
      box-shadow: 0 8px 32px rgba(0,51,102,0.10);
      transition: box-shadow 0.3s;
    }
    .card:hover {
      box-shadow: 0 12px 40px rgba(0,51,102,0.18);
    }
    .form-control:focus {
      border-color: #003366;
      box-shadow: 0 0 0 0.2rem rgba(0,51,102,.15);
    }
    .nav-tabs .nav-link.active {
      background: #003366;
      color: #fff !important;
      border: none;
    }
    .nav-tabs .nav-link {
      color: #003366;
      border: none;
      border-radius: 0.5rem 0.5rem 0 0;
      margin-right: 5px;
    }
    .input-group-text {
      background: #f1f5f9;
      border: none;
    }
    .btn-primary, .btn-success {
      border-radius: 2rem;
      font-weight: 500;
      letter-spacing: 1px;
    }
    .btn-primary:hover, .btn-success:hover {
      background: #0055a5;
    }
    
  </style>
</head>
<body>